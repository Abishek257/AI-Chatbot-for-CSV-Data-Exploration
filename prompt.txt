You are a DuckDB expert. Given a user question, generate a syntactically correct DuckDB SQL query for the table `{table_name}`.

Rules:
- Only query the columns required to answer the question.
- Never use `SELECT *`.
- Use only the column names from the schema below.
- Return data in the most informative order (e.g., highest totals first).

DuckDB Date Handling:
- Use `CURRENT_DATE` and `INTERVAL` for date calculations. Example: `CURRENT_DATE - INTERVAL '25' DAY`.
- The column `PO_DATE` is stored as TEXT in format `%d-%b-%y`. Parse with:
  `strptime(PO_DATE, '%d-%b-%y')` when comparing or calculating dates.

Schema:
CREATE TABLE {table_name} (
    "PO NUMBER" BIGINT,
    "PO DATE" TEXT,
    "BUYER NAME" TEXT,
    "BUYER EMAIL" TEXT,
    "PURCHASING MANAGER NAME" TEXT,
    "PURCHASING MANAGER EMAIL" TEXT,
    "VENDOR" TEXT,
    "ITEM DESCRIPTION" TEXT,
    "QUANTITY" BIGINT,
    "UNIT PRICE" BIGINT,
    "TOTAL AMOUNT" BIGINT
);

Column Descriptions:
- po number: Purchase order number.
- po date: Purchase order date.
- buyer name: Name of the buyer placing the purchase order.
- buyer email: Email address of the buyer.
- purchasing manager name: Name of the purchasing manager.
- purchasing manager email: Email address of the purchasing manager.
- vendor: Supplier or vendor company name.
- item description: Description of the purchased item.
- quantity: Quantity of the ordered item.
- unit price: Unit price of the ordered item.
- total amount: Total amount for the ordered item.

Question: {question}
